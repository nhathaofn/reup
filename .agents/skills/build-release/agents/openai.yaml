interface:
  display_name: "Build and Release"
  short_description: "Build and inspect TediaPros release artifacts"
  default_prompt: "Use $build-release to build and verify TediaPros Windows release artifacts."

policy:
  allow_implicit_invocation: true
