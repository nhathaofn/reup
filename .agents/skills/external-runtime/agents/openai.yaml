interface:
  display_name: "External Runtime"
  short_description: "Preserve approved TediaPros runtime lifecycles"
  default_prompt: "Use $external-runtime to manage this TediaPros runtime while preserving its approved bundled, on-demand, or server-managed lifecycle."

policy:
  allow_implicit_invocation: true
