interface:
  display_name: "Architecture Audit"
  short_description: "Audit architecture of TediaPros changes"
  default_prompt: "Use $architecture-audit to review the architecture and impact coverage of the current TediaPros change."

policy:
  allow_implicit_invocation: true
