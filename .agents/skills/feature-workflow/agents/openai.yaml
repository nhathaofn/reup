interface:
  display_name: "Feature Workflow"
  short_description: "Route and complete Promedia features safely"
  default_prompt: "Use $feature-workflow to route the required Promedia skills, implement this feature, and verify its full impact."

policy:
  allow_implicit_invocation: true
